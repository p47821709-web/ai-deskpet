export function usePetApi() {
  return {
    fetchPets: async () => [],
    getPet: async (id: string) => null,
    createPet: async (data: any) => null,
    deletePet: async (id: string) => {},
  }
}
